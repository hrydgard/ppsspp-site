# DLL files missing

One user has reported that on his Windows machine, PPSSPP complains about a missing MFPlat.dll.

The workaround, confirmed by this user, is to, in Windows settings, go to `Settings` > `System` > `Optional features` > `Add features`, then search for `Media feature pack` and add it.
